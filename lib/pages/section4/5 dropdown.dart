import 'package:flutter/material.dart';

class DropDown5 extends StatelessWidget {
  const DropDown5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "DropDown",
          style: TextStyle(color: Colors.white),
        ),
        backgroundColor: Colors.teal,
      ),
        // centerTitle: true,
      body: ),
  }
}
